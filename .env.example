# EIP-7702 Inspector Configuration
# Copy this file to .env and customize for your environment
#
# Priority Order:
#   1. CLI flags (highest priority)
#   2. Environment variables (.env)
#   3. Chain presets (-preset flag)
#   4. Default values (lowest priority)

# =============================================================================
# Chain Configuration
# =============================================================================

# Chain preset (optional)
# Available presets: local, mainnet, sepolia, holesky, goerli
# Run `./eip7702-inspector -list-presets` to see all available presets
# CHAIN_PRESET=sepolia

# Custom chain ID (overrides preset if set)
# Examples:
#   1        = Ethereum Mainnet
#   11155111 = Sepolia Testnet
#   17000    = Holesky Testnet
#   31337    = Local (Anvil/Hardhat)
# CHAIN_ID=11155111

# RPC URL (overrides preset if set)
# Examples:
#   http://localhost:8545              = Local node (default)
#   https://rpc.sepolia.org            = Sepolia public RPC
#   https://eth.llamarpc.com           = Mainnet public RPC
# RPC_URL=https://rpc.sepolia.org

# =============================================================================
# Test Account Configuration
# =============================================================================

# Private key for test account (WITHOUT 0x prefix)
# WARNING: Never commit real private keys to version control!
# WARNING: Use only test accounts with no real value!
# PRIVATE_KEY=your_private_key_here_without_0x_prefix

# Account address (optional - derived from private key if not set)
# ADDRESS=0x...

# Target address for delegation (contract to delegate to)
# This is the smart contract address that will receive delegation
# TARGET_ADDRESS=0x...

# =============================================================================
# Usage Examples
# =============================================================================

# Example 1: Sepolia Testnet with preset
#   CHAIN_PRESET=sepolia
#   PRIVATE_KEY=your_test_key
#
#   Run: ./eip7702-inspector -network

# Example 2: Custom RPC with environment variables
#   CHAIN_ID=11155111
#   RPC_URL=https://your-custom-rpc.com
#   PRIVATE_KEY=your_test_key
#
#   Run: ./eip7702-inspector -network

# Example 3: Local development with Anvil
#   CHAIN_PRESET=local
#   PRIVATE_KEY=ac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80
#
#   Run: anvil & ./eip7702-inspector -network
